{% from "govuk/components/select/macro.njk" import govukSelect %}

{{ govukSelect({
    name: 'subType',
    id: 'subType',
    label: {
        text: "Sub-type"
    },
    items: subTypes | addDefaultSelectedVale('Select', true),
    errorMessage: errors | findError(subType)
}) }}
