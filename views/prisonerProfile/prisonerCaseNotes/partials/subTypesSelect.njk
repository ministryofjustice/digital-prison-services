{% from "govuk/components/select/macro.njk" import govukSelect %}

{{ govukSelect({
    name: 'subType',
    id: 'subType',
    label: {
        text: "Sub-type"
    },
    items: subTypes | addDefaultSelectedVale('Select') | setSelected(subTpe),
    errorMessage: errors | findError(subType)
}) }}
