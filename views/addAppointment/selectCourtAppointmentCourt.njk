{% extends "../partials/layout.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "govuk/components/select/macro.njk" import govukSelect %}

{% set title = "Date and time is available" %}
{% set mainClasses = "govuk-main-wrapper--l" %}

{% block content %}
{% if errors.length > 0 %}
    {{ govukErrorSummary({
        titleText: "There is a problem",
        errorList: errors,
        attributes: { 'data-qa-errors': true }
    }) }}
{% endif %}

<h1 class="govuk-heading-l">{{title}}</h1>

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        {{ govukSummaryList({
            classes: "govuk-summary-list--no-border",
            rows: details | toSummaryViewModel
        }) }}
    </div>
</div>

<form method="post">
  {{ govukSelect({
    name: "court",
    id: "court",
    label: {
        text: "What court are you in?"
    },
    items: courts | addDefaultSelectedVale('Select'),
    errorMessage: errors | findError("court")
  }) }}

  <p class="govuk-body">
    Now you need to select the rooms in the prison.
  </p>

  {{ govukButton({
      text: "Continue",
      type: 'submit'
  }) }}
</form>
{% endblock %}
