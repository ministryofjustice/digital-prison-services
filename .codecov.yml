coverage:
  status:
    project:
      default:
      backend:
        flags: backend
      frontend:
        flags: frontend
flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - src/
comment:
  layout: "diff,flags,tree"
  behavior: new
  require_changes: no
