coverage:
  status:
    project:
      backend:
        flags: backend
      frontend:
        flags: frontend
flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - src/
comment:
  layout: "diff,flags,tree"
  behavior: default
  require_changes: no
